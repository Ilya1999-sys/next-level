---
description: Hover and match videos must be 1080/2K, cover the card, autoplay, and match the story
alwaysApply: true
---

# Highlight videos

- Minimum **1080p**, prefer **2K**.
- Fill the card and the match stage (`object-fit: cover` or a 2K iframe scaled to cover).
- On card hover, muted autoplay starts at once. No extra click.
- The clip must be that **year, tournament, and match** from the card title.

Prefer a local file in `public/media/highlights/` (`<video>`, no ads). If the file is missing, play the matching embed so hover still works.

```ts
// BAD: local-only path with no file → hover plays nothing
{ src: "/media/highlights/portugal-wales-2016.mp4" }

// GOOD: local file when present, embed fallback so hover always plays
{
  src: "/media/highlights/portugal-wales-2016.mp4",
  embed: { provider: "dailymotion", id: "x4jmmp8" },
}
```
